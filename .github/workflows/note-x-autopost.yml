# [非推奨・無効化済み] このワークフローは note-sns-autopost.yml（X + Instagram 統合版）に統合されました。
# 二重投稿を防ぐため、自動スケジュールを無効化しています。手動実行時も何もしません。
name: '[DEPRECATED] Note to X Auto Post'

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated notice
        run: echo "このワークフローは note-sns-autopost.yml に統合されました。何も実行しません。"
